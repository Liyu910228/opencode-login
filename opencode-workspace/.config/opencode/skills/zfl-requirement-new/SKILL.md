---
name: zfl-requirement
description: 完整的需求引导与原型 skill，用于 opencode/harness 中和用户聊清需求、澄清业务、读取目标项目上下文、撰写 requirement.md / reqdoc.md，并基于内置 snow-design-mobile 生成可预览 demo.html。Use when the user wants to 做需求分析、需求调研、需求澄清、页面需求梳理、问题驱动访谈、角色权限梳理、现有流程分析、同类页面对照、项目风格对齐、撰写需求方案、整理需求文档、生成可评审 Markdown 需求稿、生成可交互 HTML 原型，或提到 zfl-requirement、需求分析、需求调研、需求文档、需求撰写、requirement.md、reqdoc.md、demo.html。即使用户只说"帮我整理一下这个需求""写个需求文档""做个需求方案""根据这个需求做个演示页"，也应触发本 skill。
---

# ZFL Requirement

这是一个运行在 opencode / harness 环境中的完整需求 skill，覆盖从需求分析到需求方案文档与可交互原型的完整流程。它负责把用户的模糊表述逐步逼近为真实需求，并沉淀为结构化的 `requirement.md`、`reqdoc.md` 和可交互的 `demo.html`。

目标项目不是本 skill 的落点，而是风格与实现习惯的取证来源。生成面向某个既有项目的页面时，必须先读取目标项目的真实页面、项目规则和本地经验，再按内部设计规范 `snow-design-mobile/GUIDE.md` 完成页面设计与 demo 实现。

## 强制执行顺序

执行必须严格按以下顺序，**不能跳过、不能并行、不能颠倒**：

1. **Phase 1：需求分析与调研** → 产出 `requirement.md`
2. **Phase 2-A：需求方案文档** → 产出完整的 `reqdoc.md`
3. **Phase 2-B：可交互原型** → 产出 `demo.html`

三个阶段存在严格前置依赖：

- 必须先完成 Phase 1，拿到 `requirement.md` 的结论后，才能开始 Phase 2-A
- 必须完成完整的 `reqdoc.md`，并把页面设计构造和功能使用方式输出给用户确认后，才能开始 Phase 2-B 生成 `demo.html`
- 不允许跳过需求分析直接写方案文档，也不允许方案文档未完成就开始画 demo

如果用户直接要求"写需求文档"或"做 demo"，仍要先补齐前置阶段：先做需求分析产出 `requirement.md`，再完成 `reqdoc.md` 并确认页面设计构造和功能使用方式，最后才做 `demo.html`。只有在用户明确表示前置阶段已完成且提供了对应产物时，才可从对应阶段继续。

---

## Phase 1：需求分析与调研

所有类型的需求分析都必须使用 [references/requirement-analysis.md](./references/requirement-analysis.md)。开始 Phase 1 时先完整读取该文件，再提问、整理信息和产出 `requirement.md`。

这份规则替代旧的通用需求分析规则，适用于数据看板、传统流程、表单配置、交易、审批、AI 生成、AI 辅助等全部需求。即使需求不是数据看板，也不能回退到旧规则；要按 `requirement-analysis.md` 的提问节奏、状态标注、字段级追问、权限确认和验收写法执行。

执行时仍需遵守以下承接规则：

- 用户只给方向、口号或功能名时，不要直接写文档；先按 `requirement-analysis.md` 追问关键缺口。
- 涉及页面或原型时，第一阶段必须把业务阅读方式和页面骨架问清楚，但不要把组件、颜色、布局变体等专业设计选择抛给用户。
- 如果是 `1 到 N`，`requirement.md` 中必须增加现有流程分析；优先读取旧页面、旧需求、代码、BI、Excel、截图或原型，先还原现状，再定义改动范围。
- 如果需求要生成面向既有项目的演示页，还必须执行项目上下文采集：读取目标项目的 `AGENTS.md`、本地 `skills/`、同类真实页面和样式习惯，形成项目基线。操作细则见 [references/project-context.md](./references/project-context.md)。
- 如果用户明确要竞品分析、行业调研、最新信息，且当前环境可联网，先补充外部调研再写入文档。不要把"最新"内容当成静态知识猜测。

`requirement.md` 完成后，才能进入 Phase 2-A。

---

## Phase 2：需求方案文档与原型

Phase 2 以 Phase 1 产出的 `requirement.md` 为输入，把需求整理成 **Markdown 需求方案文档 + 可交互 HTML 原型**，便于编辑、评审、版本对比和浏览器直接查看操作。

Phase 2 内部同样有严格顺序：**先完成完整的 `reqdoc.md`（Phase 2-A），输出页面设计构造和功能使用方式给用户确认，再开始 `demo.html`（Phase 2-B）**。

### Phase 2 产物

产出两个核心文件：

| 文件 | 用途 | 说明 |
|------|------|------|
| `reqdoc.md` | 结构化需求方案文档 | **主产物**，用于编辑、版本对比、文本协作、评审签字 |
| `demo.html` | 可交互 HTML 原型 | **辅助产物**，严格按 `reqdoc.md` 和内置 `snow-design-mobile` 生成，浏览器打开即可查看页面、点击菜单、操作弹窗，用于评审展示和截图 |

如果需求涉及界面截图或流程图，还建议产出：

- `images/` 目录：存放从 `demo.html` 中截取的关键界面图，供 `reqdoc.md` 引用
- `【流程图】项目名-模块名.drawio`：流程图文件（Draw.io 格式），供 `reqdoc.md` 引用

默认要求：

- `reqdoc.md` 是权威文档，文字描述应完整自洽，不依赖 `demo.html` 即可理解全部需求
- `reqdoc.md` 完成后，必须先向用户输出页面设计构造和功能使用方式确认稿；用户确认前不得开始生成 `demo.html`
- `demo.html` 的视觉和布局输入来自 `reqdoc.md`、目标项目同类页面基线和内置 `snow-design-mobile`
- 进入 demo 实现后不得再向用户确认颜色、布局、组件选择或是否继续；必须按 `snow-design-mobile/GUIDE.md` 及其按场景路由到的 references 自行决策
- `demo.html` 应与 `reqdoc.md` 的数据、字段、文案、页面结构和交互规则严格一致，互为补充
- `demo.html` 应为自包含的单个/两个 HTML 文件，使用内联 CSS 和 JavaScript，无需外部依赖即可在浏览器中直接打开，区分移动端与PC端，移动端尺寸为375*812；PC端尺寸为 1440*1024
- 输出时要明显突出"该需求研发需要优先查看、重点注意、容易漏掉、阻塞开发、影响联调和验收"的内容，而不是把所有信息写成平均密度的平铺说明

### 生成顺序

1. **先出 `reqdoc.md`**：用 [reqdoc.template.md](./reqdoc.template.md) 作为结构骨架，填入真实需求内容。**必须完整完成 `reqdoc.md` 的 8 个一级章节后，才能进入下一步。**
2. **输出确认稿**：把 `reqdoc.md` 中的页面设计构造和功能使用方式整理给用户确认。用户确认前不得开始 `demo.html`。如果用户提出问题或反馈，先同步修正 `requirement.md` / `reqdoc.md` 中受影响内容，再重新输出确认稿，直到用户确认没问题。
3. **再出 `demo.html`**：用户确认后，参照 `reqdoc.md` 生成可交互原型。视觉风格、布局、组件、图标、状态和检查清单必须严格引用内部 `snow-design-mobile/GUIDE.md` 设计规范，并按页面场景读取其 references。
4. **交付前验证 `demo.html`**：按 `reqdoc.md` 逐项核对页面、入口、字段、状态、权限、异常、验收场景和可点击交互；能用浏览器打开验证时，必须实际点击主要按钮、Tab、筛选、弹窗、抽屉、确认/取消、返回和页面切换，确认没有遗漏、无死按钮、无明显交互断点。
5. **迭代同步**：用户反馈后，先按 [references/correction-loop.md](./references/correction-loop.md) 归并反馈、判断影响面，并像需求整理阶段一样向用户确认本轮修正范围；确认后再同步修改所有受影响产物。涉及界面截图时，从 `demo.html` 截图保存到 `images/` 目录。

### 文件命名规范

| 文档类型 | 命名格式 | 示例 |
|----------|----------|------|
| 需求方案 | `【需求文档】项目名-需求方案.md` | `【需求文档】麦宝知识后台-需求方案.md` |
| 交互原型 | `demo.html` | `demo.html`（一般放在项目根目录） |
| 流程图 | `【流程图】项目名-模块名.drawio` | `【流程图】麦宝知识后台-核心流程.drawio` |
| 截图 | `images/页面说明-视图名.png` | `images/知识类别配置-列表页.png` |
| 截图指引 | `images/README.md` | 列明每张截图的截取位置和保存文件名 |

### reqdoc.md 固定结构

`reqdoc.md` 的内容结构固定为以下 8 个一级部分，顺序不要变：

1. `需求背景与目标`
2. `需求范围`
3. `需求详情`
4. `功能开发事项`
5. `验收标准`
6. `风险与处理策略`
7. `非功能需求`
8. `待确认问题`

#### 文档头部

`reqdoc.md` 必须以版本信息头部开头：

```markdown
# 项目名 功能需求方案文档

> **版本**：v1.0
> **日期**：YYYY-MM-DD
> **适用系统**：系统名称
> **需求来源**：来源说明
> **优先级**：功能A（P0）、功能B（P1）

> **demo链接**：`路径/demo.html`
```

#### 1. 需求背景与目标

- 项目背景
- 业务目标
- 用户目标
- 本轮要解决的问题
- 不在本轮解决的问题（如有）

#### 2. 需求范围

必须包含两部分：

- `需求清单` 表格：需求名称、优先级、适用角色、关键用户旅程、是否本期必做
- `核心流程`：流程说明

核心流程要求：

- **不推荐在 Markdown 中使用 ASCII 流程图**（`┌─┐│└┘` 等字符），因为不同编辑器/浏览器显示容易乱码
- 如果流程较复杂，推荐：
  - 用 Draw.io 绘制并保存为 `【流程图】项目名-模块名.drawio`，在文档中引用文件路径
  - 在文档中用文字概述流程的关键节点、分支和异常路径
- 只要能让读者快速看清主流程、分支、判断条件、异常路径和结束结果即可

#### 3. 需求详情

这一部分必须 **按需求范围逐项展开**，并且要结合 UI 图一起说明。

**优先规则**：

- 如果用户提供了 MasterGo 交互图、页面图、截图、导出图或可读取链接，优先结合这些材料输出
- 如果当前环境不能直接读取 MasterGo 原稿，不要假装读过；要明确说明，并改用用户提供的截图、导出图、页面说明或 `ui.md`
- 如果已有 `ui.md`，要把 `ui.md` 中的页面结构、交互、状态说明一起合并进这一部分

**每条需求详情至少说明**：

- 对应需求项
- 适用角色与权限
- 触发入口（**必须明确主入口和辅助入口**）
- 页面与视图（引用 `demo.html`，预留截图占位）
- 交互步骤
- 关键状态（建议用表格）
- 业务规则
- 数据输入输出
- 异常或边界情况
- 开发必看（高亮块）
- 重点注意（高亮块）
- 风险提醒（高亮块）

##### 触发入口写法规范

必须明确区分主入口和辅助入口，禁止模糊表述（如"本期先做A，B作为P1"）。

**正确示例**：

```
**入口 1 — 批量同步（本期主入口）**：列表顶部「xxx」按钮，支持多选后批量触发
**入口 2 — 单条同步（辅助入口）**：列表操作列「同步」按钮，方便快速单条触发
```

##### UI 图占位规范

- 在 `reqdoc.md` 中预留图片引用：`![描述](images/截图名称.png)`
- 截图文件名与需求章节对应，如：`images/知识类别配置-列表页.png`、`images/知识类别配置-新增编辑弹窗.png`
- 截图从 `demo.html` 中截取，保存到 `images/` 目录
- 建议创建 `images/README.md` 截图指引文件

##### 页面与视图写法规范

```markdown
> **界面原型**：参考 `demo.html` → 按原型中的页面入口查看。以下为关键界面说明：

**xxx 页面**：

![xxx-列表页](images/xxx-列表页.png)

- 页面标题：
- 操作按钮：
- 列表字段：
  | 字段 | 说明 |
  |------|------|
  | ... | ... |
```

##### 字段一致性要求

- 如果在需求详情中新增、拆分或修改了字段（如将"FastGPT 文件夹"拆分为"名称+地址"），必须同步检查并更新文档中**所有引用该字段的位置**
- 需要同步更新的位置包括：列表字段表、表单字段表、唯一性校验规则、业务规则、数据输入输出说明、交互步骤示例、后端接口说明、验收场景、风险描述、待确认问题
- 不允许在文档中出现同一字段前后描述不一致的情况

#### 4. 功能开发事项

从产品方案视角整理给研发的开发关注点，用表格呈现：

- 前端开发事项
- 后端开发事项
- AI 开发事项（若为 AI 需求）
- 接口或数据依赖
- 状态管理或流程编排事项
- 埋点、日志、消息、通知、权限等补充事项

#### 5. 验收标准

按需求项或流程节点列出，用表格呈现：

| 验收场景 | 前置条件 | 操作步骤 | 期望结果 | 失败判定 |
|----------|----------|----------|----------|----------|
| ... | ... | ... | ... | ... |

AI 需求可额外增加「AI 评测集」列。

#### 6. 风险与处理策略

用表格呈现，至少包含：

- 需求理解风险
- 交互或流程风险
- 技术依赖风险（含 ECM/外部接口并发、超时等）
- 数据一致性风险
- 排期风险
- 对应处理策略

#### 7. 非功能需求

用表格呈现，至少覆盖适用项：

- 性能（含接口超时时间、列表查询时间等具体数值）
- 安全
- 稳定性
- 易用性
- 可维护性
- 兼容性
- 可观测性

#### 8. 待确认问题

用表格呈现，**必须包含"确认结果"列**：

| 问题描述 | 影响范围 | 需要谁确认 | 不确认的风险 | 确认结果 |
|----------|----------|------------|--------------|----------|
| ... | ... | ... | ... | 待确认 |

**待确认问题的迭代规则**：

- 当问题得到答案后，必须在"确认结果"列标注 `**已确认：xxx**`
- 确认后，必须**同步更新需求详情中受影响的章节**（如确认"本期做批量同步"，需同步修改 3.x 触发入口的描述、开发必看块、接口设计说明等）
- 确认结果不是装饰，是推动文档迭代的信号

### demo 前页面方案确认

完整生成 `reqdoc.md` 后，必须先把页面设计构造和功能使用方式输出给用户确认。确认的是**页面怎么组织、功能怎么使用、业务流程是否符合预期**，不是颜色、圆角、卡片样式、Tab 视觉变体等专业视觉选择。

确认稿必须包含：

- **页面清单**：本次 demo 会包含哪些页面、弹窗、抽屉、状态页。
- **页面结构**：每个页面从上到下的模块顺序，例如顶部栏、筛选、核心摘要、列表/表格、详情、底部操作。
- **功能入口**：主入口、辅助入口、批量入口、单条入口、返回路径分别在哪里。
- **使用流程**：用户从进入页面到完成主要任务的操作步骤。
- **关键交互**：Tab、筛选、搜索、列表点击、详情查看、新增/编辑、提交、确认、取消、错误提示、toast 等交互。
- **字段与状态**：核心字段、表单字段、列表字段、状态流转、空/加载/错误/无权限/禁用等状态。
- **权限和边界**：不同角色能看什么、能点什么、不能做什么，异常或边界情况如何展示。
- **待确认点**：只列会影响业务理解、页面结构、功能入口、数据字段、权限或验收的问题。

确认稿输出后必须停在确认点，不能直接生成 `demo.html`。如果用户提出问题或反馈，先判断影响面并同步修改文档：

- 影响需求背景、目标、范围、用户角色、权限、现有流程、核心问题、目标/非目标、风险或待确认项时，必须更新 `requirement.md`。
- 影响页面结构、功能入口、使用流程、字段、状态、交互、业务规则、异常边界、开发事项或验收标准时，必须更新 `reqdoc.md`。
- 同一条反馈同时影响需求事实和页面方案时，必须同时更新 `requirement.md` 和 `reqdoc.md`，不能只改其中一个。

修正后重新输出简版确认稿，继续等待用户确认；用户未确认前不得进入 `demo.html` 生成阶段。只有用户明确说"按这个来""确认""没问题""继续做 demo"，才视为确认。

### demo.html 原型规范

`demo.html` 是一个可交互的 HTML 原型文件，用于直观展示需求界面和交互流程。**只有在 `reqdoc.md` 完整完成，且用户已确认页面设计构造和功能使用方式后，才能开始生成 `demo.html`。**

#### 设计规范来源

生成 `demo.html` 时，视觉规范必须严格引用内部 `snow-design-mobile/GUIDE.md` 设计规范。应优先读取 `snow-design-mobile/GUIDE.md`，并按页面场景读取其 `references/` 目录下的对应规范文件：

- **基础视觉规范**：`snow-design-mobile/references/foundations.md`
- **通用页面结构**：`snow-design-mobile/references/page-guidelines.md`、`snow-design-mobile/references/layout-rules.md`
- **看板页 / 数据看板 / 月报 / 日报 / 经营分析 / 库存分析 / 销量分析 / 风险监控**：`snow-design-mobile/references/dashboard-pattern.md`、`snow-design-mobile/references/layout-rules.md`
- **Banner、图标、组件**：按需读取 `snow-design-mobile/references/dashboard-banners.md`、`snow-design-mobile/references/icon-rules.md`、`snow-design-mobile/references/component-rules.md`
- **页面检查或评审**：`snow-design-mobile/references/checklist.md`

所有移动端 H5 原型必须遵循 Snow Design-Mobile 的业务优先、信息清晰、紧凑可扫读、交互可预期原则。

设计实现阶段不得打扰用户。进入 demo 实现后，遇到颜色、布局、卡片、Tab 位置、组件变体、图标和状态样式等选择时，必须优先依据 `reqdoc.md`、目标项目同类页面、项目画像和 Snow Design-Mobile 规则自行决策。只有事实性缺口真正阻塞实现时，才回到需求整理阶段补问。

#### 结构与交互

- 自包含的单个 HTML 文件（内联 CSS + JS），无需外部依赖
- 按 `reqdoc.md` 的业务场景和 Snow Design-Mobile 页面类型选择页面承载方式：移动端 H5 优先使用 375*812 手机画布；PC 管理后台才使用左侧导航、表格和分页
- 每个页面展示完整界面布局：顶部栏、筛选区、核心信息区、主体内容、状态反馈和必要操作
- 支持弹窗（Modal）、抽屉（Drawer）等交互组件
- 按钮和链接可点击，有基本交互反馈（弹窗打开、页面切换、toast 提示）
- 弹窗内表单需覆盖全量字段，展示两列布局等真实排版

#### 数据与文案一致性

- 表格数据、表单字段、下拉选项与 `reqdoc.md` **严格一致**
- 统计图表数据使用合理的示例数据，**百分比必须同时标注具体数量**（如：质量 46%（12个））
- 提示文案、确认框文案与需求文档一致
- 不使用 Lorem ipsum 等占位文本，用真实场景数据

#### 交付前验证

生成 `demo.html` 后，必须先按 `reqdoc.md` 自检再交付，不能只看首屏或静态截图。

- **需求范围核对**：逐项检查 `需求清单`、`需求详情`、`页面与视图`、`交互步骤`、`关键状态`、`业务规则`、`异常或边界情况`、`验收标准`，确认 demo 中都有对应页面、入口、字段、状态或交互表达。
- **点击链路核对**：实际打开 `demo.html`，点击主入口、辅助入口、Tab、筛选、搜索、列表项、表格横滚、详情、弹窗、抽屉、确认、取消、返回、提交、toast 等所有在 `reqdoc.md` 中出现的交互。
- **状态覆盖核对**：至少覆盖默认态、空态、加载态、错误态、无权限态、禁用态、成功反馈、失败反馈；如果某状态不适用，要能从 `reqdoc.md` 找到理由。
- **字段和文案核对**：列表字段、表单字段、下拉选项、按钮文案、确认框文案、错误提示、单位、百分比和数量标注必须与 `reqdoc.md` 一致。
- **Snow Design-Mobile 核对**：按 `snow-design-mobile/references/checklist.md` 及页面场景对应 references 检查布局、组件、图标、表格、Banner、移动端可用性和视觉一致性。
- **修复闭环**：发现 demo 漏功能、漏字段、按钮点不开、交互无反馈、状态缺失或与 `reqdoc.md` 不一致时，先修 `demo.html`；如果是 `reqdoc.md` 本身缺失或口径错误，先反写 `reqdoc.md` 再同步修 demo。

#### 与 reqdoc.md 联动

- 当需求发生变更（新增字段、调整交互入口、修改数据展示方式）时，**同步更新 `demo.html`**
- 当页面结构、模块顺序、布局骨架、状态表达、项目基线发生变更时，**先同步更新 `reqdoc.md` 中页面与视图、交互步骤、关键状态、验收标准等受影响章节，再更新 `demo.html`**
- 当需求方案中预留了界面截图占位时，按占位要求从 `demo.html` 截取对应界面保存到 `images/` 目录
- 在 `reqdoc.md` 的"页面与视图"小节标注"参考 `demo.html` → 某菜单/按钮查看"

### 与 `ui.md` 的关系

如果 `ui.md` 已存在，更新 `reqdoc.md` / `demo.html` 时要同步修正页面、交互、状态说明，避免几份文档内容冲突。

### 迭代与修改规则

需求文档从 v1.0 开始，用户反馈驱动迭代。每次迭代遵循以下规则：

1. **先归并反馈并确认修正批次**：不要用户说一点就立刻改一点；先整理成待修正清单、影响面和本轮建议处理范围，再请用户确认
2. **再判断反馈层级**：按 [references/correction-loop.md](./references/correction-loop.md) 区分业务理解、字段口径、权限流程、页面结构、风格规则和 demo bug
3. **先改上游产物**：业务与规则问题先改 `requirement.md` / `reqdoc.md`，再同步 `demo.html`
4. **修改字段时做全局一致性检查**：确认所有引用该字段的位置已同步更新
5. **确认问题后联动更新**：待确认问题获答复后，在确认结果列记录，并同步修改受影响章节
6. **版本号递增**：每次正式修改后更新版本号和日期
7. **截图同步**：界面变更后，重新从 `demo.html` 截图替换 `images/` 中的旧图

### 推荐落地方式

1. 先以 `reqdoc.template.md` 为骨架写出 `reqdoc.md` 结构化正文
2. 输出页面设计构造和功能使用方式确认稿；如用户反馈，修正 `requirement.md` / `reqdoc.md` 后重新确认，直到用户确认没问题
3. 同步生成 `demo.html` 可交互原型，严格引入内部 `snow-design-mobile/GUIDE.md` 及按场景路由到的 references
4. 打开 `demo.html`，按 `reqdoc.md` 和 Snow Design-Mobile 检查清单实际点击验证，修完遗漏和断点后再交付
5. 把当前项目的真实需求内容填进去，用真实数据而非占位符
6. 如果用户提供了品牌样式、公司规范、截图、MasterGo、`ui.md`，在不打乱 8 个一级章节顺序的前提下补充
7. 如果需求较复杂，允许在一级章节下增加二级分组，但不要改动一级章节名称
8. 在 `reqdoc.md` 中优先增加以下高亮区域：
   - 开发必看
   - 重点注意
   - 风险提醒
9. 高亮不是装饰，要承载真实信息，不能只写空话或重复正文
10. 同步创建 `images/` 目录和截图指引文件
11. 如果流程复杂，同步创建 `.drawio` 流程图文件，避免在 Markdown 中写 ASCII 流程图
12. 数据描述务必具体：百分比标注绝对数量、时间标注到年-月-日 时:分、接口超时标注具体秒数
